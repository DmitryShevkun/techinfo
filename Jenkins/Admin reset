Отключаем security:
 - открываем файл
 
sudo nano /var/lib/jenkins/config.xml

 - меняем tru на false( либо копируем строку )
 
<useSecurity>false</useSecurity>

 - рестартим jenkins
 
sudo service jenkins restart

 - подключаемся к gui и настраиваем security заново
